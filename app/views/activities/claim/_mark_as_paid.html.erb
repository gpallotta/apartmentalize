<div class="activity-icon">
  <span class="fui-checkmark-24"></span>
  <%= parse_time(activity.updated_at) %>
</div>

<div class="activity-content">
  <%= activity.owner.name %> marked <%= link_to claim.title, claim_path(claim) %>
  as paid
</div>
